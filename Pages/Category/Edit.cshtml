@page
@model Back_Gestion.Pages.Category.EditModel

@{
    ViewData["Title"] = "Edit";
}

<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h4 style="text-align:center" class="m-0 font-weight-bold text-primary">Modification d'une Catégorie</h4>
    </div>
    <div class="card-body">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Categorie.id" />
            <div class="form-group">
                <label asp-for="Categorie.libelle" class="control-label"></label>
                <input asp-for="Categorie.libelle" class="form-control" />
                <span asp-validation-for="Categorie.libelle" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input style="float:right" type="submit" value="Modifier" class="btn btn-info" />
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
